# pnpm dev 에서만 읽는다. 비밀값이 아니라 번들에 그대로 박히는 값이라 커밋한다.
# 로컬은 5173(프론트)과 8080(백엔드)이 서로 다른 오리진이라 절대 주소가 필요하다.
# 둘 다 localhost 라 same-site 로 쳐주기 때문에 SameSite=Lax 쿠키도 실린다.
VITE_API_BASE_URL=http://localhost:8080
